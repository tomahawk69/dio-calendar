<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd"
         version="3.1">

    <!-- TODO: + rmi->jax-rs -->

    <!--TODO: + Hibernate through annotations (Entry) -->
    <!--TODO: + constraints for Entry (required etc) -->
    <!--TODO: + id as primary key -->
    <!--TODO: + table for attenders and attenders to standalone class -->
    <!--TODO: look on Spring JDBC templates (spring-data, spring-jdbc) -->

    <!-- TODO: + search by subject not session (?) -->
    <!-- TODO: + new entry -->
    <!-- TODO: + entry edit (standalone page) -->
    <!-- TODO: + ui:composition -->
    <!-- TODO: - entry edit and collapse table on index.xhtml (ui:param) -->
    <!-- TODO: - validation standard and custom -->
    <!-- TODO: - login service -->
    <!-- TODO: - ExceptionHandler -->
    <!-- TODO: - sorting -->

    <!-- TODO: + version with show/hide to branch and send to Vasyl -->
    <!-- TODO: + Primefaces standard validation -->
    <!-- TODO: + Hybernate instead of XML -->




    <display-name>calendar-client</display-name>
    <context-param>
        <param-name>javax.faces.PROJECT_STAGE</param-name>
        <param-value>Development</param-value>
    </context-param>
    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>classpath:clientApplicationContext.xml</param-value>
    </context-param>
    <context-param>
        <param-name>primefaces.CLIENT_SIDE_VALIDATION</param-name>
        <param-value>true</param-value>
    </context-param>
    <context-param>
        <param-name>primefaces.TRANSFORM_METADATA</param-name>
        <param-value>true</param-value>
    </context-param>

    <welcome-file-list>
        <welcome-file>index.xhtml</welcome-file>
    </welcome-file-list>

    <listener>
        <listener-class>com.sun.faces.config.ConfigureListener</listener-class>
    </listener>

    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>

    <servlet>
        <servlet-name>Faces Servlet</servlet-name>
        <servlet-class>javax.faces.webapp.FacesServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>Faces Servlet</servlet-name>
        <url-pattern>*.xhtml</url-pattern>
    </servlet-mapping>


</web-app>